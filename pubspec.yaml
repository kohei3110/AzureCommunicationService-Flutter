name: azurecommunicationserviceflutter
description: A new Flutter project.

publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=2.18.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  firebase_messaging: ^14.2.1
  firebase_core: ^2.4.1
  flutter_local_notifications: ^13.0.0
  firebase_analytics: ^10.1.0
  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  hooks_riverpod:

  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true